{
  "name": "schedura-monorepo",
  "private": true,
  "devDependencies": {
    "turbo": "1.10.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test"
  }
}